[package]
name = "mermaid-ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
boltffi = "0.30"
mermaid-rs-renderer = { version = "0.3.1", default-features = false }
